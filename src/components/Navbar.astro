---
// src/components/Navbar.astro
---
<style is:global>
        body {
                background-color: #252525;
        }

        #colorPrincipal{
                background-color: #313338;
        }

        #sidebar{
                width: 30rem;
                background-color: #313338;
                z-index: 1000;
        }

        @keyframes fadeIn {
                from {
                        opacity: 0;
                }
                to {
                        opacity: 1;
                }
        }

        @keyframes handwriting {
                0% {width: 0;}
                100% {width: 100%;}
        }

        .colorPrincipal {
                color: rgb(229, 255, 0);
                position: relative;
                overflow: hidden;
                white-space: nowrap;
                -webkit-text-stroke: .5px black;
                font-weight: bold;
        }

        .colorPrincipal2 {
                letter-spacing: 1em;
                font-size: 2em;
                color: rgb(229, 255, 0);
                position: relative;
                overflow: hidden;
                white-space: nowrap;
                -webkit-text-stroke: .5px black;
                font-weight: bold;
        }

        .colorPrincipal3 {
                letter-spacing: .3em;
                font-size: 1em;
                color: rgb(229, 255, 0);
                position: relative;
                overflow: hidden;
                white-space: nowrap;
                -webkit-text-stroke: .5px black;
                font-weight: bold;
        }

        .colorPrincipal::after {
                content: attr(data-text);
                position: absolute;
                top: 0;
                left: 0;
                width: 0;
                height: 100%;
                color: rgb(229, 255, 0);
                overflow: hidden;
                animation: handwriting 2s steps(40, end) forwards;
        }

        #closeIcon {
                stroke: white;
                transition: stroke 0.7s ease;
        }

        #closeIcon:hover {
                stroke: rgb(229, 255, 0);
                text-shadow: 0 0 2px rgb(229, 255, 0), 0 0 2px rgb(229, 255, 0), 0 0 2px rgb(229, 255, 0), 0 0 2px rgb(229, 255, 0);
        }

        #menuIcon {
                transition: stroke 0.7s ease;
        }

        #menuIcon:hover {
                stroke: rgb(229, 255, 0);
        }

        .alturaW{
                margin-top: -10%;
        }

        .titulos{
                font-weight: bold;
                text-align: center;
                text-decoration: underline;
        }

        .pagina{
                background-color: #ffffff;
                height: 70.16rem;
                max-width: 54rem;
                margin: auto;
                padding: 4rem;
                padding-top: 5rem;
                margin-top: 5rem;
                margin-bottom: 2rem;
                user-select: none;
                border-width: 1px;
                border-color: rgb(209 213 219);
                position: relative;
        }

        .pie-pagina {
                position: absolute;
                bottom: 0;
                left: 0;
                width: 100%;
                height: 5rem;
                background-color: #ffffff;
                display: flex;
                justify-content: center;
                align-items: center;
        }

        .pagina::after {
                content: "Project W";
                position: absolute;
                font-weight: bold;
                bottom: 3em;
                right: .5em;
                color: rgba(0, 0, 0, 0.1);
                font-size: 10rem;
                transform: rotate(-45deg);
        }

        .tituloNormativa{
                font-size: 3.5rem;
                line-height: 4rem;
                margin-bottom: 5rem;
        }

        .subtitulos1{
                margin-left: 2rem;
                font-style: italic;
                font-weight: bold;
                text-decoration: underline;
        }

        ::-webkit-scrollbar {
                width: 10px;
                background-color: #313338;
        }

        ::-webkit-scrollbar-track {
                background-color: #313338;
        }

        ::-webkit-scrollbar-thumb {
                background-color: #555;
        }

        ::-webkit-scrollbar-thumb:hover {
                background-color: #e5ff00;
        }


        @media (max-width: 768px) {
                .pagina {
                        height: 72rem;
                        padding: 2rem;
                        padding-top: 3rem;
                        margin-top: 3rem;
                        margin-bottom: 1rem;
                }

                .pagina::after {
                        font-size: 8em;
                        margin-bottom: 1em;
                }

                .tituloNormativa {
                        font-size: 3rem;
                        line-height: 3rem;
                        margin-bottom: 4rem;
                }

                #sidebar {
                        width: 40%;
                }
        }

        @media (max-width: 390px) and (max-height: 844px) {
                .pagina {
                        height: 120rem;
                        padding: 2rem;
                        padding-top: 2rem;
                        margin-top: 2rem;
                        margin-bottom: 1rem;
                }

                .pagina::after {
                        font-size: 5em;
                        margin-bottom: 4em;
                        margin-right: -2em;
                }

                .tituloNormativa {
                        font-size: 1.8rem;
                        margin-bottom: 2em;
                        line-height: 2rem;
                }
        }

</style>

<div id="colorPrincipal" class="animate-slide-in-top min-h-20 flex justify-between h-auto bg-gray-950 px-4 py-3">
        <a href="/">
                <div class="flex my-3 select-none" id="logo">
                        <span class="alturaTexto colorPrincipal font-bold"></span>
                        <img id="logoProject" alt="Logo Project W" class="alturaW max-w-10 max-h-10 ml-1 hidden" src="/project-w.svg">
                </div>
        </a>
        <button id="toggleMenu" class="text-white focus:outline-none">
                <svg id="menuIcon" class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                </svg>
                <svg id="closeIcon" class="h-6 w-6 hidden transform" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                </svg>
        </button>
</div>

<div id="sidebar" class="text-white w-64 h-screen fixed top-15 right-0 overflow-y-auto hidden">
        <div class="px-4 py-3">
                <a href="/normativa" class="block text-white py-2 transition duration-500 ease-out hover:text-yellow-400">Normativa General</a>
                <a href="/normativa-lspd" class="block text-white py-2 transition duration-500 ease-out hover:text-yellow-400">Normativa LSPD</a>
                <a href="#" class="block text-white py-2 transition duration-500 ease-out hover:text-yellow-400">Contacto</a>
        </div>
</div>

<script>
        window.onload = function() {
                const text = 'Project';
                const span = document.querySelector('.colorPrincipal');
                let index = 0;
                const logo = document.getElementById("logoProject");

                function writeText() {
                        if (span) {
                                if (index < text.length) {
                                        span.innerHTML += text[index];
                                        index++;
                                        setTimeout(writeText, 250);
                                } else if (index == text.length && logo) {
                                        setTimeout(logoW, 200)
                                        function logoW() {
                                                if (logo) {
                                                        logo.classList.remove('hidden');
                                                        logo.style.animation = "fadeIn 0.35s ease-in-out";
                                                }
                                        }
                                }
                        }
                }

                writeText();
        }

        const toggleMenu = document.getElementById('toggleMenu');
        const sidebar = document.getElementById('sidebar');
        const menuIcon = document.getElementById('menuIcon');
        const closeIcon = document.getElementById('closeIcon');

        if (toggleMenu && sidebar && menuIcon && closeIcon) {
                toggleMenu.addEventListener('click', function(event) {
                        event.stopPropagation();
                        sidebar.classList.toggle('hidden');
                        menuIcon.classList.toggle('hidden');
                        closeIcon.classList.toggle('hidden');
                });
        }

        document.body.addEventListener('click', function(event) {
                const targetNode = event.target instanceof Node ? event.target : null;
                if (sidebar && targetNode && !sidebar.contains(targetNode) && menuIcon && closeIcon) {
                        sidebar.classList.add('hidden');
                        menuIcon.classList.remove('hidden');
                        closeIcon.classList.add('hidden');
                }
        });

        window.addEventListener('wheel', function(event){
                if (sidebar && menuIcon && closeIcon) {
                        sidebar.classList.add('hidden');
                        menuIcon.classList.remove('hidden');
                        closeIcon.classList.add('hidden');
                }
        })

        window.addEventListener('scroll', function(event){
                if (sidebar && menuIcon && closeIcon) {
                        sidebar.classList.add('hidden');
                        menuIcon.classList.remove('hidden');
                        closeIcon.classList.add('hidden');
                }
        })
</script>
